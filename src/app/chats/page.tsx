"use client";

import ChatLayout from "~/components/chat-layout";

// Keep the original simple structure, ChatLayout handles everything
export default function ChatsPage() {
  return <ChatLayout />;
}
